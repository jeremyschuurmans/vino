<% unless current_user?(@wine.user) %>
  <div id="follow_form">
    <% if current_user.following?(@wine.user) %>
      <%= render 'users/unfollow_button' %>
    <% else %>
      <%= render 'users/follow_button' %>
    <% end %>
  </div>
<% end %>
<br></br>
