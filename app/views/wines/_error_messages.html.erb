<% if @wine.errors.any? %>
  <div id="error_message">
    <div class="alert alert-danger">
      Oops. <%= pluralize(@wine.errors.count, "error") %> occurred.
    </div>
    <ul>
      <%= @wine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
